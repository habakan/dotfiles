# Development layout: nvim (left 60%) | claude (right 40%)
# Use current pane's directory for both new panes
split-window -h -p 40 -c "#{pane_current_path}"
select-pane -L
send-keys "nvim" Enter
select-pane -R
send-keys "claude" Enter
